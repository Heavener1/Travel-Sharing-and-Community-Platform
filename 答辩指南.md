# 旅游分享与交流平台 — 毕业设计答辩指南

## 一、项目概述（2分钟讲完）

**一句话定义**：一个基于 Django REST Framework + Vue 3 的全栈旅游分享与交流平台，集成 Elasticsearch 搜索引擎、AI 大模型（Kimi / 千问）智能助手，以及基于用户行为的协同推荐系统。

**核心价值**：
- 解决传统旅游平台"信息过载、决策困难"的问题
- 通过搜索引擎 + AI 智能导览帮助用户快速找到心仪景点
- 通过社区互动（发帖、评论、点赞）+ 行为追踪实现个性化推荐

---

## 二、系统架构（答辩重点，建议画图展示）

### 2.1 总体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端层 (Vue 3 + Vite)                  │
│    路由管理 / Pinia 状态管理 / SSE 流式渲染                │
├─────────────────────────────────────────────────────────┤
│                 HTTP REST API + SSE                      │
├─────────────────────────────────────────────────────────┤
│              后端 API 层 (Django REST Framework)          │
│           JWT 鉴权 / 标准化 JSON 响应封装                  │
│     ┌──────────┬──────────┬──────────┬───────────┐      │
│     │ 用户模块  │ 旅游模块  │ 社区模块  │ 行程规划   │      │
│     │ 注册/登录 │ 景点/酒店 │ 帖子/评论 │ AI 生成    │      │
│     └──────────┴──────────┴──────────┴───────────┘      │
│     ┌──────────────────────────────────────────┐        │
│     │         AI 模块 (Kimi / 千问)              │        │
│     │   行程顾问 / 帖子润色 / 景点分析 / 智能问答  │        │
│     └──────────────────────────────────────────┘        │
├─────────────────────────────────────────────────────────┤
│                     数据与服务层                           │
│  MySQL ─── Redis ─── Elasticsearch ─── MinIO ─── AI API │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术栈选型

| 层次 | 技术 | 选型理由 |
|------|------|----------|
| 后端框架 | Django REST Framework | JWT 鉴权、序列化、分页开箱即用；ORM 成熟 |
| 数据库 | MySQL (远程) | 关系型数据，景点-酒店-评价多表关联查询 |
| 缓存 | Redis (远程) | 图形验证码缓存、Session、高并发读取 |
| 搜索引擎 | Elasticsearch (Docker) | 中文分词搜索，比 MySQL LIKE 快 10x+ |
| 对象存储 | MinIO (远程) | S3 兼容，存图片/封面，预签名 URL 安全访问 |
| AI 模型 | Kimi / 千问 | 双模型热切换，SSE 流式输出实时打字效果 |
| 前端 | Vue 3 + Vite + Pinia | 组件化开发，响应式状态管理 |
| 容器化 | Docker Compose | 一键启动 ES + 后端 + 前端 |

---

## 三、功能模块详解（答辩核心，每模块3分钟）

### 3.1 用户模块（apps/users/）

**技术要点**：
- **JWT 双 Token 机制**：Access Token（8小时）+ Refresh Token（7天），无状态鉴权
- **图形验证码**：PIL 生成 4 位随机码，存 Redis 300 秒过期，Register/Login 时必须校验
- **安全设计**：密码 Django 内置哈希存储，验证码防暴力破解

**讲解话术**：
"用户模块采用 JWT 鉴权方案，相比传统 Session-Cookie 模式，JWT 是无状态的，不需要在服务器端存储会话，更适合前后端分离架构。同时我加入了图形验证码机制，防止注册和登录接口被恶意脚本攻击。"

### 3.2 旅游模块（apps/travel/）

**技术要点**：
- **智能搜索（SmartSearchView）**：三路并发 —— Elasticsearch 全文检索 + MySQL 模糊匹配兜底 + AI 智能总结
- **SSE 流式搜索（SmartSearchStreamView）**：Server-Sent Events 实时推送搜索结果，用户可见逐步加载过程
- **个性化推荐**：基于用户行为（浏览/点赞/收藏/发帖）构建偏好画像，加权排序
- **内容关联推荐**：基于标签相似度 + 地理相似度 + 用户偏好亲和度三维打分

**核心算法（个性化排序）**：
```
景点得分 = 用户行为权重 + 标签匹配度×0.8 + 城市匹配度×0.8 + 省份匹配度×0.5 + 原始评分
行为权重：收藏(8) > 评价(6+评分) > 行程(4) > 发帖(4) > 点赞(3) > 浏览(1)
```
**讲解话术**：
"搜索模块我设计了三层架构：第一层 Elasticsearch 负责高效全文检索，支持中文分词；第二层 MySQL 作为容错兜底，万一 ES 挂了系统不会崩溃；第三层接入 AI 大模型，根据候选结果生成自然语言的搜索建议，给用户'有人帮你整理信息'的体验。"

### 3.3 社区模块（apps/social/）

**技术要点**：
- **帖子审核机制**：创建默认 "pending" 状态，管理员审核后 "approved" 才公开展示
- **多级评论**：PostComment 通过 `parent` 自引用外键实现嵌套回复
- **通知系统**：点赞/评论/回复三种事件触发通知，标记已读/未读
- **内容审核**：ModerationSummaryView 提供审核队列仪表盘

**讲解话术**：
"社区模块引入了内容审核机制，所有帖子创建后默认为'待审核'状态，只有管理员通过后才对外展示。这模拟了真实互联网产品的内容安全策略。评论系统采用自引用外键实现多级嵌套，通过 Django ORM 的 prefetch_related 预加载避免 N+1 查询问题。"

### 3.4 行程规划模块（apps/planner/）

**技术要点**：
- **智能行程生成（TripGeneratorView）**：根据出发地、目的地、天数、预算、偏好自动匹配景点
- 支持偏好标签模糊匹配（如 "自然风光" 匹配到 tags 中包含相关词的景点）
- 按照 `is_hidden_gem`（小众）和评分排序，优先推荐高质量景点

**讲解话术**：
"行程规划的亮点是它不只是一个表单提交，而是做了智能匹配——用户输入目的地和偏好后，系统会通过 ORM 的条件查询找到相关景点，并按照小众优先、高评分优先的规则自动生成每日行程建议。"

### 3.5 AI 模块（apps/ai/）

**技术要点**：
- **双模型热切换**：前端可选 Kimi 或千问，后端统一 Provider 抽象层
- **SSE 流式输出**：所有 AI 接口支持 SSE（Server-Sent Events），实现 ChatGPT 式逐字输出效果
- **四种 AI 功能**：
  | 功能 | 用途 |
  |------|------|
  | 旅行顾问 | 基于行程参数生成完整旅行建议 |
  | 帖子润色 | 帮助用户润色标题、正文、标签 |
  | 帖子总结 | 综合帖子内容 + 评论生成摘要 |
  | 景点问答 | 基于景点资料回答用户问题 |
  | 景点分析 | 基于评分数据 + 评价生成数据分析 |

**讲解话术**：
"AI 模块是整个项目最大的亮点。我设计了一个统一的 Provider 抽象层，支持 Kimi 和千问两个大模型热切换。所有 AI 接口都采用 SSE（Server-Sent Events）流式输出，前端可以实时展示 AI 的生成过程，就像 ChatGPT 那样逐字打印，而不是等 10 秒一次性返回。这个模式比传统的请求-响应模式体验好很多。"

---

## 四、答辩时如何讲解（建议流程，15-20分钟）

### 第一阶段：背景与动机（2分钟）
- "随着旅游消费升级，用户需要的不再是简单的景点列表，而是个性化的旅行建议和真实用户分享。"
- "这个项目旨在将搜索、推荐、AI 助手和社区分享整合到一个平台中。"

### 第二阶段：系统演示（8分钟，最重要！）
1. 打开前端首页，展示目的地浏览和搜索
2. **演示智能搜索**（输入一个关键词，展示 ES + DB + AI 三路结果，这是最大亮点）
3. 点击景点详情，展示评分分布、酒店信息、相关推荐
4. 进入社区，展示帖子列表、发帖、评论
5. **演示 AI 润色**（在发帖弹窗中点击 AI 润色，展示流式输出效果）
6. **演示行程规划**（输入出发地/目的地，生成行程）
7. **演示 AI 旅行顾问**（在行程页选择模型，展示流式生成）
8. 展示个人仪表盘（统计信息）

### 第三阶段：技术架构讲解（5分钟）
- 展示架构图（建议提前画好 PPT）
- 重点讲三层搜索架构 + AI 集成 + 推荐算法
- 强调技术选型的 tradeoff（为什么用 MySQL 而不是 MongoDB、为什么用 JWT 而不是 Session）

### 第四阶段：总结与展望（2分钟）
- 项目已实现的核心功能
- 未来可优化的方向（移动端适配、移动端 App、更多 AI 模型）

---

## 五、答辩常见技术问题及回答策略

### Q1: 为什么选择 Django REST Framework 而不是 Spring Boot / Node.js？

**回答**：DRF 的优势在于开发效率 —— 序列化器、认证、分页、权限都是开箱即用的。Python 生态在 AI/数据处理方面也有天然优势，比如我的 AI 模块需要调用大模型 API，Python 是 AI 领域的首选语言。如果项目需要更高并发，后续可以用 gunicorn + gevent 做协程部署，Docker Compose 配置中已经预置了 gunicorn。

### Q2: 你的推荐算法是怎么实现的？是不是真正的协同过滤？

**回答**：我实现的是**基于内容的混合推荐**，核心思路是给用户行为赋予不同权重，构建用户偏好画像（标签偏好、城市偏好、省份偏好、景点偏好），然后对候选景点和帖子加权打分排序。它结合了标签相似度、地理相似度和用户历史行为亲和度三个维度。不是传统的矩阵分解协同过滤，但对于毕业设计的数据规模来说，这种基于权重的方案更实用、可解释性更强。

### Q3: JWT Token 过期了怎么办？安全性怎么保证？

**回答**：我采用了双 Token 机制 —— Access Token（8小时）用于日常请求，Refresh Token（7天）用于无感刷新。Access Token 过期后，前端用 Refresh Token 调用 `/api/auth/refresh/` 接口换取新的 Access Token。Refresh Token 也过期才需要重新登录。安全性方面：Token 通过 HTTPS 传输（生产环境），使用 Bearer 认证头，图形验证码防暴力破解。

### Q4: SSE 和 WebSocket 有什么区别？为什么用 SSE？

**回答**：SSE 是单向的（服务器 → 客户端），WebSocket 是双向的。我的 AI 流式输出场景只需要服务器推送数据给前端，不需要前端频繁发消息，所以 SSE 更合适。SSE 的优势在于：基于 HTTP 协议，不需要额外握手，天然支持自动重连，实现简单。WebSocket 更适合即时通讯这种需要双向通信的场景。

### Q5: 为什么要同时用 Elasticsearch 和 MySQL？不是冗余吗？

**回答**：MySQL 是主数据存储，Elasticsearch 是搜索引擎。两者定位不同：MySQL 存全量数据（景点详情、用户信息、帖子内容），ES 只存搜索需要的字段（名称、城市、标签、简介）。搜索时走 ES 的全文检索（中文分词、TF-IDF 评分），拿到 ID 后再回 MySQL 查出完整数据。如果 ES 挂了，系统会降级到 MySQL LIKE 查询，保证可用性。这是典型的**读写分离 + 搜索引擎辅助**架构。

### Q6: MinIO 是什么？为什么要用？

**回答**：MinIO 是 S3 兼容的对象存储服务，部署在我自己的远程服务器上。它的优势：1）API 兼容 AWS S3，后续迁移云服务零成本；2）支持预签名 URL，上传和下载都可以生成临时访问链接，安全可控；3）比直接存文件系统更好扩展。头像和景点封面通过预签名 URL 访问，避免了直接暴露存储路径。

### Q7: 如何处理高并发场景？比如大量用户同时搜索？

**回答**：项目中做了几层优化：1）Redis 缓存——验证码、热点数据缓存在 Redis 中，减轻数据库压力；2）Elasticsearch 搜索——比 MySQL 全文搜索效率高出 10 倍以上；3）数据库查询优化——使用 `select_related` 和 `prefetch_related` 减少 N+1 查询；4）分页——所有列表接口都做了分页（每页 10 条）；5）生产环境部署用 gunicorn 多进程 + Nginx 反向代理，可以水平扩展。

### Q8: 你的 AI 模块是怎么设计的？如果 API Key 没钱了怎么办？

**回答**：设计上做了 Provider 抽象层，Kimi 和千问有统一的调用接口（`chat_completion` / `chat_completion_stream`）。系统启动时会检查 API Key 是否配置，前端可以查看可用模型列表。如果某个 Provider 不可用，前端会显示"未配置"，不会报 500 错误。流式输出通过 SSE 实现，后端使用 `requests` 库的 stream 模式逐行解析 OpenAI 兼容的响应格式。

### Q9: 数据库表是怎么设计的？有哪些关联关系？

**回答**：
- User ↔ UserProfile（一对一）：用户基本信息与扩展资料分离
- Destination ↔ Hotel（一对多）：一个景点有多家酒店
- Destination ↔ DestinationReview（一对多）：景点评价，加了唯一约束防重复评价
- Post ↔ PostComment（一对多，自引用）：帖子评论，parent 字段实现嵌套回复
- TripPlan ↔ TripStop（一对多）：行程包含多个停靠点
- UserAction（行为日志）：记录用户的浏览/点赞/收藏等行为，是推荐系统的数据基础
- Notification（通知）：关联接收者、触发者、帖子、评论

### Q10: 测试数据怎么来的？你怎么验证功能正确性？

**回答**：项目中实现了 `batch_seed` 管理命令和 `AdminBatchSeedView` API，可以批量生成测试账号、景点、帖子。`seed_demo` 管理命令预置了 7 个国内热门景点作为演示数据。管理员可以在管理后台的仪表盘看到数据统计和趋势图。单元测试方面，每个 App 下有 `tests.py`，覆盖了模型创建和 API 基本调用。

### Q11: 为什么要用 Docker？你的 Docker 编排做了什么？

**回答**：Docker 解决的是环境一致性问题。我的项目依赖 MySQL（远程）、Redis（远程）、MinIO（远程）、Elasticsearch（本地 Docker），如果不用 Docker，评委老师本地跑起来需要手动装 ES。Docker Compose 编排了三容器：Elasticsearch 先启动（带健康检查），后端容器依赖 ES 健康后启动（自动执行 migrate + 种子数据 + 创建管理员），前端 Nginx 容器最后启动。一条命令 `docker compose up` 就能跑起来。

### Q12: 你的项目相比同类产品有什么创新点？

**回答**：
1. **三路智能搜索**：ES + MySQL + AI 三条检索通道并行，市面上旅游平台极少同时整合
2. **双 AI 模型热切换**：Kimi 和千问可随时切换，不绑定单一服务商
3. **基于行为的个性化推荐**：不是简单的"热门推荐"，而是根据用户实际浏览、点赞、收藏行为动态调整
4. **SSE 流式体验**：AI 对话采用 SSE 实时推送，实现 ChatGPT 式交互体验
5. **完整的内容审核流程**：模拟了真实产品的审核-通过-拒绝机制

---

## 六、答辩 PPT 建议结构

1. **封面**：项目名称 + 姓名 + 导师 + 日期
2. **项目背景**：1-2 页，旅游行业数据 + 痛点分析
3. **功能概览**：1 页，功能模块总览图
4. **技术架构**：2 页，架构图 + 技术栈表格
5. **核心模块**：5-6 页，每个模块一页 + 关键代码截图
6. **AI 集成**：1-2 页，双模型 + SSE 流式原理
7. **搜索与推荐**：1 页，三路搜索 + 推荐算法
8. **系统演示**：现场操作，10 分钟
9. **总结与展望**：1 页
10. **Q&A**：1 页

---

## 七、答辩前最后的检查清单

- [ ] 远程 MySQL / Redis / MinIO 是否正常运行？
- [ ] Elasticsearch Docker 是否能启动？
- [ ] AI API Key 是否有效？额度是否够用？
- [ ] 电脑上能否跑通完整流程（注册 → 登录 → 搜索 → 详情 → 发帖 → AI 润色 → 行程规划）？
- [ ] 网络是否稳定？（远程服务器连接不能断）
- [ ] 浏览器是否无缓存模式打开？（避免 JS 缓存导致的 bug）
- [ ] 准备好演示账号（demo / demo123456）和解说用的景点关键词
- [ ] 有没有备用方案？（如果远程服务挂了，是否能切到本地 SQLite？）
